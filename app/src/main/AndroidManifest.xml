<?xml version="1.0" encoding="utf-8"?>
<!--version="1.0"说明xml的版本是1.0，encoding="utf-8"说明的是xml传输数据时候用的字符编码-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a12541.weatherforecast">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--
        AndroidManifest文件中有四个标签与permission有关，它们分别是 <permission>、<permission-group> 、<permission-tree> 和 <uses-permission>
        其中最常用的是 <uses-permission>，当我们需要获取某个权限的时候就必须在我们的manifest文件中声明<uses-permission>
    -->

    <application
        android:name="cn.edu.pku.qixiangyang.app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SelectCity">
        </activity>

    </application>

</manifest>
